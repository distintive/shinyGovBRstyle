% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_step.R
\name{br_step}
\alias{br_step}
\title{GovBR step}
\usage{
br_step(inputId, labels, initial = 1)
}
\arguments{
\item{inputId}{The input slot that will be used to access the step
index.}

\item{labels}{Character vector of step labels.}

\item{initial}{Initially active step (1-based).}
}
\value{
A \code{shiny.tag} with the step markup and GovBR dependencies
  attached.
}
\description{
Creates a \code{br-step} progress indicator.
\code{input$<inputId>} returns the index (1-based) of the last clicked
(or initial) step.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_step(
        "etapas", c("Identifica\u00e7\u00e3o", "Dados", "Confirma\u00e7\u00e3o")
      ),
      shiny::verbatimTextOutput("valor")
    )
  )
  server <- function(input, output, session) {
    output$valor <- shiny::renderPrint(input$etapas)
  }
  shinyApp(ui = ui, server = server)
}
}
