% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_radio_input.R
\name{br_radio_input}
\alias{br_radio_input}
\title{GovBR radio input}
\usage{
br_radio_input(
  inputId,
  label,
  choices = NULL,
  selected = NULL,
  hint = NULL,
  inline = FALSE,
  heading_level = NULL
)
}
\arguments{
\item{inputId}{The input slot that will be used to access the value.}

\item{label}{Group label shown above the options.}

\item{choices}{Vector of values. Use a named vector to display labels
different from the values.}

\item{selected}{Initially selected value (\code{NULL} for none).}

\item{hint}{Auxiliary help text shown below the label.}

\item{inline}{If \code{TRUE}, options are laid out horizontally.}

\item{heading_level}{Optional heading level ("2"-"6") for the group
legend (screen readers announce it as the group label).}
}
\value{
A \code{shiny.tag} with the radio group markup and GovBR
  dependencies attached.
}
\description{
Creates a group of \code{br-radio} options bound as a standard Shiny
radio group: \code{input$<inputId>} returns the selected value.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_radio_input(
      inputId = "uf",
      label = "Estado",
      choices = c("Distrito Federal" = "DF", "São Paulo" = "SP")
    ),
    shiny::verbatimTextOutput("valor")
  )
  server <- function(input, output, session) {
    output$valor <- shiny::renderPrint(input$uf)
  }
  shinyApp(ui = ui, server = server)
}
}
