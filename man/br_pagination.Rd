% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_pagination.R
\name{br_pagination}
\alias{br_pagination}
\title{GovBR pagination}
\usage{
br_pagination(inputId, total, current = 1)
}
\arguments{
\item{inputId}{The input slot that will be used to access the page
number.}

\item{total}{Total number of pages.}

\item{current}{Initially selected page (1-based).}
}
\value{
A \code{shiny.tag} with the pagination markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-pagination} control. \code{input$<inputId>} returns
the current page number.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shiny::uiOutput("pagina"),
      shiny::verbatimTextOutput("valor")
    )
  )
  server <- function(input, output, session) {
    output$pagina <- shiny::renderUI(
      shinyGovBRstyle::br_pagination("pag", total = 5,
                                    current = input$pag \%||\% 1)
    )
    output$valor <- shiny::renderPrint(input$pag)
  }
  shinyApp(ui = ui, server = server)
}
}
