% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_card.R
\name{br_card}
\alias{br_card}
\title{GovBR card}
\usage{
br_card(..., title = NULL, footer = NULL, hover = FALSE)
}
\arguments{
\item{...}{UI elements placed in the card content.}

\item{title}{Optional card header title.}

\item{footer}{Optional UI element placed in the card footer.}

\item{hover}{If \code{TRUE}, adds the hover emphasis effect.}
}
\value{
A \code{shiny.tag} with the card markup and GovBR dependencies
  attached.
}
\description{
Creates a \code{br-card} container with optional header, content and
footer areas.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_card(
        title = "Meu cart\u00e3o",
        shiny::p("Conte\u00fado do cart\u00e3o."),
        footer = shinyGovBRstyle::br_button("ok", "Confirmar")
      )
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
