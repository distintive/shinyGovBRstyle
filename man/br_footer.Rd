% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_footer.R
\name{br_footer}
\alias{br_footer}
\title{GovBR footer}
\usage{
br_footer(
  categories = NULL,
  logo = "govbr",
  logo_alt = "Logotipo",
  licence_text = NULL
)
}
\arguments{
\item{categories}{Optional named list of link categories. Each element
is a character vector of link labels (links point to \code{"#"}). When
\code{NULL} (default) only the logo and licence text are shown.}

\item{logo}{Path or URL for the footer logo. Use \code{"govbr"} for the
logo bundled with the package.}

\item{logo_alt}{Alternative text for the logo.}

\item{licence_text}{Text shown in the licence information area.}
}
\value{
A \code{shiny.tag} with the footer markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-footer}, the official GovBR page footer.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    shinyGovBRstyle::br_layout(shiny::h1("Conte\u00fado")),
    shinyGovBRstyle::br_footer(
      categories = list("Categoria" = c("Link 1", "Link 2"))
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
