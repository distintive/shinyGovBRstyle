% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_checkbox_input.R
\name{br_checkbox_input}
\alias{br_checkbox_input}
\title{GovBR checkbox input}
\usage{
br_checkbox_input(
  inputId,
  label,
  choices = NULL,
  selected = NULL,
  hint = NULL,
  inline = FALSE,
  heading_level = NULL
)
}
\arguments{
\item{inputId}{The input slot that will be used to access the value.}

\item{label}{Checkbox label (single) or group label shown above the
options (group).}

\item{choices}{Optional vector of values for a checkbox group. Use a
named vector to display labels different from the values.}

\item{selected}{Initially checked values (group) or \code{TRUE}/FALSE
(single).}

\item{hint}{Auxiliary help text shown below the label.}

\item{inline}{If \code{TRUE}, group options are laid out horizontally.}

\item{heading_level}{Optional heading level ("2"-"6") for the group
legend (screen readers announce it as the group label).}
}
\value{
A \code{shiny.tag} with the checkbox markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-checkbox}. For a single checkbox,
\code{input$<inputId>} returns \code{TRUE}/\code{FALSE}. When
\code{choices} is supplied a checkbox group is created instead and
\code{input$<inputId>} returns a character vector of the checked values.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_checkbox_input("aceite", "Aceito os termos"),
    shinyGovBRstyle::br_checkbox_input(
      inputId = "temas",
      label = "Temas de interesse",
      choices = c("tec" = "Tecnologia", "sau" = "Saúde"),
      selected = "tec"
    ),
    shiny::verbatimTextOutput("valor")
  )
  server <- function(input, output, session) {
    output$valor <- shiny::renderPrint(
      list(aceite = input$aceite, temas = input$temas)
    )
  }
  shinyApp(ui = ui, server = server)
}
}
