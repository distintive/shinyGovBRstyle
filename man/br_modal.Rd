% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_modal.R
\name{br_modal}
\alias{br_modal}
\title{GovBR modal}
\usage{
br_modal(
  title,
  ...,
  footer = NULL,
  size = c("medium", "small", "large"),
  id = NULL
)
}
\arguments{
\item{title}{Modal title.}

\item{...}{Modal body content.}

\item{footer}{Optional UI element for the modal footer (e.g.
\code{br_button()}).}

\item{size}{One of \code{"small"}, \code{"medium"} (default) or
\code{"large"}.}

\item{id}{Optional id for the modal element.}
}
\value{
A \code{shiny.tag} with the modal markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-modal} dialog. The close button and any footer
button created with \code{data-dismiss = "br-modal"} close the modal
via the GovBR JavaScript.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_modal(
        title = "Confirmar a\u00e7\u00e3o",
        shiny::p("Deseja realmente continuar?"),
        footer = shinyGovBRstyle::br_button("ok", "Sim", type = "primary")
      )
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
