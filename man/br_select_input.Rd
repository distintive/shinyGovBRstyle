% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_select_input.R
\name{br_select_input}
\alias{br_select_input}
\title{GovBR select input}
\usage{
br_select_input(
  inputId,
  label,
  choices = NULL,
  selected = NULL,
  multiple = FALSE,
  placeholder = "Selecione o item",
  hint = NULL
)
}
\arguments{
\item{inputId}{The input slot that will be used to access the value.}

\item{label}{Select label.}

\item{choices}{Vector of values. Use a named vector to display labels
different from the values.}

\item{selected}{Initially selected value(s).}

\item{multiple}{Whether multiple items can be selected.}

\item{placeholder}{Placeholder text for the filter field.}

\item{hint}{Auxiliary help text shown below the select.}
}
\value{
A \code{shiny.tag} with the select markup and GovBR dependencies
  attached.
}
\description{
Creates a \code{br-select}, the GovBR dropdown list. For
\code{multiple = FALSE}, \code{input$<inputId>} returns the selected
value; for \code{multiple = TRUE} it returns a character vector of the
selected values. The filter text field is available as
\code{input$<inputId>_filter}.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_select_input(
      inputId = "uf",
      label = "Estado",
      choices = c("Distrito Federal" = "DF", "São Paulo" = "SP")
    ),
    shiny::verbatimTextOutput("valor")
  )
  server <- function(input, output, session) {
    output$valor <- shiny::renderPrint(input$uf)
  }
  shinyApp(ui = ui, server = server)
}
}
