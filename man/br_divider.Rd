% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_divider.R
\name{br_divider}
\alias{br_divider}
\title{GovBR divider}
\usage{
br_divider(class = NULL)
}
\arguments{
\item{class}{Optional extra classes, e.g. \code{"vertical"},
\code{"dashed"} or spacing utilities such as \code{"my-3"}.}
}
\value{
A \code{shiny.tag} with the divider markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-divider} horizontal rule.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shiny::tags$div("Conteúdo acima"),
    shinyGovBRstyle::br_divider(class = "my-3"),
    shiny::tags$div("Conteúdo abaixo")
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
