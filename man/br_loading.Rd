% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_loading.R
\name{br_loading}
\alias{br_loading}
\title{GovBR loading indicator}
\usage{
br_loading(
  size = c("medium", "small", "large"),
  progress = NULL,
  label = "Carregando"
)
}
\arguments{
\item{size}{Spinner size: \code{"medium"} (default), \code{"small"} or
\code{"large"}.}

\item{progress}{Optional numeric value between 0 and 100. When
supplied, renders a progress bar instead of a spinner.}

\item{label}{Accessible label for screen readers.}
}
\value{
A \code{shiny.tag} with the loading markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-loading} spinner or progress bar.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_loading(),
      shinyGovBRstyle::br_loading(progress = 75)
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
