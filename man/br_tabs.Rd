% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_tabs.R
\name{br_tabs}
\alias{br_tabs}
\title{GovBR tabs}
\usage{
br_tabs(inputId, titles, ..., panels = list(...), icons = NULL)
}
\arguments{
\item{inputId}{Id for the tab container.}

\item{titles}{Character vector of tab titles.}

\item{...}{One UI element per tab, in the same order as
\code{titles} (or pass a list of elements to \code{panels}).}

\item{panels}{Alternative to \code{...}: list of UI elements.}

\item{icons}{Optional character vector of Font Awesome icon classes,
one per tab.}
}
\value{
A \code{shiny.tag} with the tab markup and GovBR dependencies
  attached.
}
\description{
Creates a \code{br-tab} navigation with content panels. The GovBR
JavaScript handles switching panels; the first tab is active initially.
\code{input$<inputId>} returns the index (1-based) of the active tab;
use \code{update_br_tabs()} to change it from the server.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_tabs(
        inputId = "abas",
        titles = c("Dados", "Gr\u00e1ficos"),
        panels = list(
          shiny::p("Conte\u00fado dos dados."),
          shiny::p("Conte\u00fado dos gr\u00e1ficos.")
        )
      )
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
