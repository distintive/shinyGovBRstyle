% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_update_page_title.R
\name{br_update_page_title}
\alias{br_update_page_title}
\title{Update the browser tab title}
\usage{
br_update_page_title(
  page_title,
  service_name = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{page_title}{New page title (single non-empty string).}

\item{service_name}{Optional service name appended after the title,
separated by \code{" | "}.}

\item{session}{The shiny session object (defaults to the current one).}
}
\value{
Invisible \code{NULL}, called for its side effect.
}
\description{
Changes the browser tab title from server code, optionally composing it
with a service name (e.g. \code{"Relat\u00f3rio | Meu Sistema"}).
Requires the GovBR dependencies (\code{use_govbr()} or any
\code{br_*} component).
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_button("rel", "Gerar relat\u00f3rio")
  )
  server <- function(input, output, session) {
    shiny::observeEvent(input$rel, {
      shinyGovBRstyle::br_update_page_title(
        page_title = "Relat\u00f3rio gerado",
        service_name = "Meu Sistema"
      )
    })
  }
  shinyApp(ui = ui, server = server)
}
}
