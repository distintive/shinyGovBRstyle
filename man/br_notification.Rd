% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_notification.R
\name{br_notification}
\alias{br_notification}
\title{GovBR notification}
\usage{
br_notification(title, subtitle = NULL, items = NULL, icons = NULL)
}
\arguments{
\item{title}{Header title.}

\item{subtitle}{Optional header subtitle (e.g. an e-mail).}

\item{items}{Character vector or list of UI elements shown as
notification items.}

\item{icons}{Optional character vector of Font Awesome icon classes,
one per item.}
}
\value{
A \code{shiny.tag} with the notification markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-notification} panel with a header and a list of
notification items.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_notification(
        title = "Fulano da Silva",
        subtitle = "nome.sobrenome@dominio.gov",
        items = c("Alerta um", "Alerta dois"),
        icons = c("fas fa-bell", "fas fa-envelope")
      )
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
