% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_accordion.R
\name{br_accordion}
\alias{br_accordion}
\title{GovBR accordion}
\usage{
br_accordion(inputId, items, single = FALSE)
}
\arguments{
\item{inputId}{Id for the accordion container.}

\item{items}{Named list. Names become the item titles and values the
item contents (can be any Shiny UI element).}

\item{single}{If \code{TRUE}, only one item can be open at a time.}
}
\value{
A \code{shiny.tag} with the accordion markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-accordion} of expandable items. Requires the GovBR
JavaScript (initialised automatically by \code{use_govbr()}).
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_accordion(
        inputId = "faq",
        items = list(
          "Assuntos" = shiny::p("Conte\u00fado de assuntos."),
          "Servi\u00e7os" = shiny::p("Conte\u00fado de servi\u00e7os.")
        )
      )
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
