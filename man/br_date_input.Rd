% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_date_input.R
\name{br_date_input}
\alias{br_date_input}
\title{GovBR date input}
\usage{
br_date_input(inputId, label, placeholder = "exemplo: 02/02/2024", hint = NULL)
}
\arguments{
\item{inputId}{The input slot that will be used to access the value.}

\item{label}{Date field label.}

\item{placeholder}{Placeholder text. Defaults to the GovBR example.}

\item{hint}{Auxiliary help text shown below the field.}
}
\value{
A \code{shiny.tag} with the date picker markup and GovBR
  dependencies attached.
}
\description{
Creates a GovBR date picker (\code{br-datetimepicker}). The value is
available in \code{input$<inputId>} as text in the \code{dd/mm/aaaa}
format.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_date_input(
      inputId = "data",
      label = "Data de nascimento"
    ),
    shiny::verbatimTextOutput("valor")
  )
  server <- function(input, output, session) {
    output$valor <- shiny::renderPrint(input$data)
  }
  shinyApp(ui = ui, server = server)
}
}
