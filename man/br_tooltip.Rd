% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_tooltip.R
\name{br_tooltip}
\alias{br_tooltip}
\title{GovBR tooltip}
\usage{
br_tooltip(
  ...,
  text,
  subtext = NULL,
  place = c("left", "top", "bottom", "right"),
  type = c("info", "success", "warning", "error")
)
}
\arguments{
\item{...}{The trigger element (e.g. a \code{br_button()}).}

\item{text}{Tooltip main text.}

\item{subtext}{Optional tooltip secondary text.}

\item{place}{Placement relative to the trigger: \code{"top"},
\code{"bottom"}, \code{"left"} (default) or \code{"right"}.}

\item{type}{Tooltip type: \code{"info"} (default), \code{"success"},
\code{"warning"} or \code{"error"}.}
}
\value{
A \code{shiny.tag} wrapping the element with the tooltip markup
  and GovBR dependencies attached.
}
\description{
Wraps a UI element with a \code{br-tooltip}. Requires the GovBR
JavaScript (initialised automatically by \code{use_govbr()}).
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_tooltip(
        shinyGovBRstyle::br_button("btn", "Ajuda"),
        text = "Texto de ajuda",
        subtext = "Informa\u00e7\u00f5es adicionais",
        place = "bottom"
      )
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
