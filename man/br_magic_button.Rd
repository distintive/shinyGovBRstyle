% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_magic_button.R
\name{br_magic_button}
\alias{br_magic_button}
\title{GovBR magic button}
\usage{
br_magic_button(
  inputId,
  label,
  size = c("medium", "small", "large"),
  icon = NULL
)
}
\arguments{
\item{inputId}{The input slot that will be used to access the click
count.}

\item{label}{Button label (or \code{aria-label} when \code{icon} is
used).}

\item{size}{One of \code{"small"}, \code{"medium"} (default) or
\code{"large"}.}

\item{icon}{Optional Font Awesome icon class. When supplied, the button
is rendered as a circle icon button.}
}
\value{
A \code{shiny.tag} with the magic button markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-magic-button}, a floating action button. Behaves
like a Shiny action button: the server observes \code{input$<inputId>}
to count clicks.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_magic_button("novo", "Adicionar",
                                      icon = "fas fa-cart-plus")
    )
  )
  server <- function(input, output, session) {
    shiny::observeEvent(input$novo, shiny::showNotification("Clicou!"))
  }
  shinyApp(ui = ui, server = server)
}
}
