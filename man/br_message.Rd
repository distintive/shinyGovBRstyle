% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_message.R
\name{br_message}
\alias{br_message}
\title{GovBR message}
\usage{
br_message(
  title,
  body = NULL,
  type = c("info", "success", "warning", "danger"),
  dismissible = TRUE
)
}
\arguments{
\item{title}{Message title.}

\item{body}{Message body text.}

\item{type}{One of \code{"info"} (default), \code{"success"},
\code{"warning"} or \code{"danger"}.}

\item{dismissible}{If \code{TRUE}, adds a close button.}
}
\value{
A \code{shiny.tag} with the message markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-message} alert.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_message(
      title = "Sucesso.",
      body = "Seus dados foram alterados conforme preenchimento.",
      type = "success"
    ),
    shinyGovBRstyle::br_message(
      title = "Atenção.",
      body = "Não compartilhe sua senha com terceiros.",
      type = "warning"
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
