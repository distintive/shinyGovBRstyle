% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_upload.R
\name{br_upload}
\alias{br_upload}
\title{GovBR upload input}
\usage{
br_upload(inputId, label, multiple = FALSE, accept = NULL, disabled = FALSE)
}
\arguments{
\item{inputId}{The input slot that will be used to access the uploaded
files.}

\item{label}{Field label.}

\item{multiple}{Whether multiple files can be selected.}

\item{accept}{Optional character vector of accepted MIME types or
extensions.}

\item{disabled}{If \code{TRUE}, disables the input.}
}
\value{
A \code{shiny.tag} with the upload markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-upload} file field. \code{input$<inputId>} works
like a standard Shiny file input (a data frame of uploaded files). The
GovBR JavaScript displays the list of selected files.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_upload(
        inputId = "arquivo",
        label = "Envio de arquivo"
      ),
      shiny::verbatimTextOutput("valor")
    )
  )
  server <- function(input, output, session) {
    output$valor <- shiny::renderPrint(input$arquivo$name)
  }
  shinyApp(ui = ui, server = server)
}
}
