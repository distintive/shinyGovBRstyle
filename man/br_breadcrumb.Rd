% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/br_breadcrumb.R
\name{br_breadcrumb}
\alias{br_breadcrumb}
\title{GovBR breadcrumb}
\usage{
br_breadcrumb(links, home = TRUE, home_href = "#")
}
\arguments{
\item{links}{Named character vector of link labels and destinations,
e.g. \code{c("P\u00e1gina 1" = "pagina1.html", "Atual" = "#")}. The last
element is marked as the current page.}

\item{home}{If \code{TRUE} (default), prepends a home link pointing to
\code{home_href}.}

\item{home_href}{Destination of the home link.}
}
\value{
A \code{shiny.tag} with the breadcrumb markup and GovBR
  dependencies attached.
}
\description{
Creates a \code{br-breadcrumb} trail. The last link is rendered as the
current page.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    use_govbr(),
    shinyGovBRstyle::br_layout(
      shinyGovBRstyle::br_breadcrumb(
        c("Relat\u00f3rios" = "#", "Anual" = "#")
      )
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui = ui, server = server)
}
}
